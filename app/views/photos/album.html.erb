<% @album_photos.each do |photo| %>
  <%= link_to((image_tag photo[:thumbnail_url]), preview_path(thumbnail_url: photo[:thumbnail_url], 
                                                                     medium_url: photo[:medium_url]), 
                                                                     method: :post ) %>
<% end %>